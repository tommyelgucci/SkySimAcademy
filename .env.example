# Copiar a .env (o configurar como variable del build en GitHub Actions/Pages)
# para activar la analítica opcional sin cookies (ver src/analytics.js).
# Vacío/ausente = analítica desactivada, cero peticiones de red.
VITE_PLAUSIBLE_DOMAIN=
